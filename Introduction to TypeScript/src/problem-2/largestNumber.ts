function getLargestNumber(firstNum: number, secondNum: number, thirdNum: number): string {
    const largestNumber = Math.max(firstNum,secondNum,thirdNum);
    return `The largest number is ${largestNumber}.`;
};